# 模拟面试报告

**项目**：Modular RAG MCP Server
**面试时间**：2026-05-21 12:50
**面试风格**：🎯 FAST — 速攻广度型
**掷骰结果**：4
**评分**：5 / 10

---

## 一、面试记录

> ✅ 答对核心要点 | ⚠️ 方向正确但细节缺失 | ❌ 未能答出或方向错误

### 方向 1：项目综述

| 轮次 | 问题 | 候选人回答原文 | 评估 | 参考答案 |
|-----|------|-------------|------|---------|
| Q1 | 这个系统里有哪几类存储？它们各自负责什么？为什么不能只用一个？ | 我认为是两类存储，分别是文件存储还有内存型存储 | ❌ | [→ 查看](#a-存储架构) |

### 方向 2：简历深挖（无简历模式）

| 轮次 | 问题 | 候选人回答原文 | 评估 | 参考答案 |
|-----|------|-------------|------|---------|
| Q2 | 这个系统里有什么设计你觉得可以改进的？如果你重新做会怎么架构？ | 我当前是采用了传统RAG模式，RAG模式他比较大的缺点其实就是只经过了一轮的检索，但还有一些专业的场景其实不应该只是一轮检索，而是多轮检索例如通过agentic rag多轮检索由LLM去判断材料是否足够，如果不够再去进行检索知道LLM认为可以输出。另外一种就是Gragh Rag, 这个方向就是可以像Graph那样展开联想发散，例如用主谓宾的方式去扩展完善整个思维，最终收敛到LLM，由LLM去总结输出给用户 | ✅ | [→ 查看](#a-改进方向) |

### 方向 3：技术深挖

| 轮次 | 问题 | 候选人回答原文 | 评估 | 参考答案 |
|-----|------|-------------|------|---------|
| Q3 | 如果要横向扩展这个系统支持多租户，你觉得哪里需要改动？最难的点是什么？ | 如果是多租户的话，那么就需要做到数据隔离，需要建立租户表来区分租户。而RAG的文档上传、向量化、入库都需要带上租户id，分别进行各自的持久化存储。最后在用户检索的时候，也要按照租户进行检索。我认为最难的点是可能有极端复杂的定制化需求 | ⚠️ | [→ 查看](#a-多租户) |
| Q4 | RRF 公式是什么？k 值怎么选？为什么不用线性加权？ | 这个不会 | ❌ | [→ 查看](#a-rrf公式) |

---

## 二、参考答案

### <a id="a-存储架构"></a>Q1: 这个系统里有哪几类存储？它们各自负责什么？为什么不能只用一个？

**参考答案**：
本项目共使用 **4 类存储**，Local-First 零外部数据库依赖：

| 存储 | 路径 | 用途 |
|------|------|------|
| Chroma | `data/db/chroma/` | Dense 向量 + metadata（chunk 文本、标题、标签等） |
| BM25 | `data/db/bm25/` | 稀疏倒排索引（词频、文档频率 IDF、chunk_id 映射） |
| ingestion_history.db | `data/db/` | 文件级 SHA256 处理记录，实现幂等去重 |
| image_index.db | `data/db/` | image_id → 文件路径映射，检索命中后取出原始图片 |

**为什么不能只用一个**：Chroma 存向量，BM25 存倒排索引，两者数据结构完全不同——Chroma 是向量数据库做 ANN 检索，BM25 是文本搜索引擎做关键词匹配，无法合并。ingestion_history.db 做文件级去重，image_index.db 做图片路径映射，各自解耦独立。

---

### <a id="a-改进方向"></a>Q2: 这个系统里有什么设计你觉得可以改进的？

**参考答案**：
候选人提到的 Agentic RAG 和 Graph RAG 确实是两个主流扩展方向：

- **Agentic RAG**：当前系统是单轮检索（用户查询 → 检索 → LLM 生成 → 返回），Agentic RAG 引入多轮 Agent 循环：LLM 判断检索材料是否充分 → 不充分则重新构造检索词 → 再次检索 → 直到 LLM 判定可以生成最终答案。类似 ReAct 或 Tool-Use 模式。
- **Graph RAG**：基于知识图谱的检索增强，通过实体-关系-实体的图结构做联想发散检索。相比传统 RAG 只匹配文本片段，Graph RAG 可以沿图边找到间接相关但语义不同构的信息。

两者都是对当前系统「单轮检索」局限的改进。

---

### <a id="a-多租户"></a>Q3: 多租户需要改动哪里？

**参考答案**：
候选人方向正确（数据隔离、租户 ID 透传），但未涉及具体存储层细节：

1. **Chroma**：为每个租户创建独立 Collection（如 `tenant_a_docs`、`tenant_b_docs`），或统一 Collection 加 `tenant_id` metadata 过滤
2. **BM25**：按租户分目录存储倒排索引（`data/db/bm25/{tenant_id}/`）
3. **ingestion_history.db**：表增加 `tenant_id` 列，查询时加租户过滤
4. **image_index.db**：同上，图片按租户隔离存储
5. **检索层**：Hybrid Search / Reranker 需透传 `tenant_id`，查询时加租户 scope 限制
6. **MCP Server**：Tool 签名增加 `tenant_id` 参数

最难的点不是"定制化需求"，而是**跨存储的一致性保障**——删除一个租户时需要协调删除 4 路存储的数据，任何一路遗漏都会造成脏数据或数据泄露。

---

### <a id="a-rrf公式"></a>Q4: RRF 公式是什么？k 值怎么选？为什么不用线性加权？

**参考答案**：

$$Score_{RRF}(d) = \frac{1}{k + Rank_{Dense}(d)} + \frac{1}{k + Rank_{Sparse}(d)}$$

- **k 的含义**：平滑因子，防止排名靠前的文档分数被过度高估。k = 60 是 Cormack et al. 2009 论文的经验推荐值；调大 k → 分布更均匀（减弱头部文档优势），调小 k → 分数差异更大。
- **为什么不用线性加权**：BM25 分数无上界，余弦相似度在 [-1, 1]，两路量纲不同——线性加权必须先归一化且引入额外超参。RRF 只依赖排名（序数信息），天然无需归一化，鲁棒性更强——无论两路各返回多少条、分数分布如何，RRF 直接对排名取倒数融合。

---

## 三、简历包装点评

本次面试**无简历**，跳过此节。

---

## 四、综合评价

**优势**：
- Q2 表现出对 RAG 领域进阶方向的了解（Agentic RAG、Graph RAG），能清晰地用类比（Graph 像思维发散）解释复杂概念，表达流畅

**薄弱点**：
- Q1 将系统 4 类存储误判为「文件存储 + 内存型存储」，未提及 Chroma、BM25、SQLite 等具体存储组件及其分工——这是项目工程骨架的核心知识点
- Q3 多租户方向正确但偏宏观，未深入到跨存储一致性等实现层面
- Q4 直接无法作答，RRF 是本项目 Hybrid Search 的核心算法，公式、k 参数含义、与线性加权的对比是高频面试必考点

**面试官建议**：
- 建议熟记项目的 4 类持久化存储及其路径和用途，能说出「为什么不能合并」
- RRF 公式必须背下来并理解 k 参数的平滑作用，这是 RAG 面试几乎必问的算法点
- 多租户分析可以更具体到「跨存储一致性」这个真正难点，而非笼统的「定制化需求」

---

## 五、评分

| 维度 | 分数（满分 10）| 评分依据 |
|-----|--------------|---------|
| 项目架构掌握 | 4 | Q1 答错存储类型（说成文件+内存，实际是 Chroma/BM25/SQLite 四类），未展现出对各存储职责的理解 |
| 简历真实性 | — | 无简历，不评分 |
| 算法理论深度 | 3 | Q4 RRF 完全不会，该项目核心融合算法未能答出；Q2 的 Agentic/Graph RAG 虽是扩展方向但属于概念性讨论 |
| 实现细节掌握 | 4 | Q3 多租户方向对但偏浅，未提跨存储一致性；Q1 无法说出具体存储组件 |
| 表达清晰度 | 7 | 回答有逻辑、使用类比解释复杂概念（Graph 像思维发散），语言流畅 |
| **综合** | **5** | 表达能力强但技术深度不足，核心算法（RRF）和存储架构是明显短板 |

---

*本场面试共 4 题，面试官风格 FAST（速攻广度型），无追问。*
*实际考察题目：Q1（第 7 题）、Q2（无简历题库第 4 题）、Q3（D 组第 4 题）、Q4（A 组第 1 题）*
